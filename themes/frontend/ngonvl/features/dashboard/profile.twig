{% extends "_layout.twig" %}

{% block content %}
<div class="container">
    <div class="row">
        <div class="col-xs-12 col-sm-6 col-md-6">
            <div class="well well-sm">
                <div class="row">
                    {#<div class="col-sm-6 col-md-4">
                        <img src="{{user.user_image_facebook_url}}" alt="" class="img-rounded img-responsive" />
                    </div>#}
                    <div class="col-sm-6 col-md-8 profile" >
                        <p><i class="fa fa-user" aria-hidden="true"></i>  Tên hiển thị :  {{user.display_name}}</p>
                        <p><i class="glyphicon glyphicon-envelope"></i>  Email : {{user.email}}</p>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

{% endblock %}
